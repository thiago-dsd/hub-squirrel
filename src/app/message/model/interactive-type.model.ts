export enum InteractiveType {
    list = "list",
    button = "button",
    product = "product",
    productList = "product_list",
    catalogMessage = "catalog_message",
    flow = "flow",
}

export enum ReceivedInteractiveType {
    buttonReply = "button_reply",
    listReply = "list_reply",
    order = "order",
}
