export interface ListReplyData {
    id: string; // ID of the button reply (required)
    title: string; // Title of the button reply (required)
    description?: string; // Description of the button reply (optional)
}
